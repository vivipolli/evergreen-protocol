import { WalletMultiButton } from '@solana/wallet-adapter-react-ui';

export default function WalletButton() {
  return (
    <WalletMultiButton className="!bg-earth-500 hover:!bg-earth-600 !text-white !rounded-md !px-4 !py-2 !text-sm !font-medium" />
  );
} 